<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

<!--    <uses-feature android:glEsVersion="0x00020000" android:required="true" />-->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
<!--    <uses-permission android:name="android.permission.TURN_SCREEN_ON" />-->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SYSTEM_EXEMPTED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
<!--    <uses-permission android:name="android.permission.USE_FINGERPRINT" />-->
<!--    <uses-permission android:name="android.permission.USE_BIOMETRIC" />-->

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
<!--    This is automatically added by the billing library -->
<!--    <uses-permission android:name="com.android.vending.BILLING" />-->

<!--    <uses-sdk android:minSdkVersion="24"/>-->

    <!-- this is included in a Google dependency and we *do not* want it. -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" tools:node="remove"/>
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- signature-level permission that is never actually granted; declaring it
         only makes the app appear in Developer options > Select mock location
         app. The real gate is the MOCK_LOCATION app op set by that picker.
         Lint's MockLocation check assumes this is test-only scaffolding and
         wants it in src/debug. It has to ship in the release manifest instead:
         the picker lists an app only if the INSTALLED build declares the
         permission, and it excludes debuggable builds, so a debug-only
         declaration would make the "sync device location with oldest provider"
         feature impossible to enable for real users. -->
    <uses-permission android:name="android.permission.ACCESS_MOCK_LOCATION"
        tools:ignore="MockLocation" />

    <!-- for tv support -->
    <uses-feature android:name="android.hardware.touchscreen"
        android:required="false" />
    <uses-feature android:name="android.hardware.screen.portrait"
        android:required="false" />
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />

    <!-- prevent these from ever being added by a dependency -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" tools:node="remove"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" tools:node="remove"/>
    <uses-feature android:name="android.hardware.location.gps"
        android:required="false" tools:node="remove"/>




    <!--    android:logo="@drawable/logo_by"-->
    <!-- see https://developer.android.com/privacy-and-security/security-config -->
    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:banner="@mipmap/ic_banner"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BringYour"
        tools:targetApi="34"
        android:networkSecurityConfig="@xml/network_security_config"
        >

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <meta-data
            android:name="asset_statements"
            android:resource="@string/asset_statements" />
        <property android:name="android.net.PROPERTY_SELF_CERTIFIED_NETWORK_CAPABILITIES" android:resource="@xml/self_certified_network_capabilities"/>

        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>

            <!-- see https://developer.android.com/training/app-links/deep-linking -->
            <!-- see https://developer.android.com/training/app-links/verify-android-applinks -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="http" />
                <data android:scheme="https" />
                <data android:host="ur.io" />
                <data android:pathPrefix="/c" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="ur" />
            </intent-filter>
            <!-- long-press launcher shortcuts: Connect / Disconnect -->
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <activity
            android:name=".MainActivity"
            android:windowSoftInputMode="adjustResize"
            android:exported="false">
        </activity>

        <!-- no-UI trampoline behind the launcher shortcuts and the widgets' quick connect button -->
        <activity
            android:name=".QuickConnectActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:taskAffinity=""
            android:theme="@android:style/Theme.NoDisplay">
            <intent-filter>
                <action android:name="com.bringyour.network.action.QUICK_CONNECT" />
                <action android:name="com.bringyour.network.action.QUICK_DISCONNECT" />
                <action android:name="com.bringyour.network.action.QUICK_TOGGLE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- Home Screen widgets (Jetpack Glance): dashboard, provider globe, contracts -->
        <receiver
            android:name=".widgets.DashboardWidgetReceiver"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_dashboard_info" />
        </receiver>
        <receiver
            android:name=".widgets.ProviderGlobeWidgetReceiver"
            android:exported="true"
            android:label="@string/site_app_providers">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_globe_info" />
        </receiver>
        <receiver
            android:name=".widgets.ContractsWidgetReceiver"
            android:exported="true"
            android:label="@string/site_app_contracts">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_contracts_info" />
        </receiver>

        <service
            android:name=".MainService"
            android:directBootAware="true"
            android:permission="android.permission.BIND_VPN_SERVICE"
            android:exported="true"
            android:label="@string/app_name"
            android:foregroundServiceType="systemExempted"
            tools:ignore="ForegroundServicePermission">

            <!-- Android explicitly permits systemExempted for VPN apps
                 configured through system VPN settings. Lint currently models
                 only the alternative exact-alarm eligibility branch; do not
                 request an unrelated alarm permission merely to satisfy it. -->

            <intent-filter>
                <action android:name="android.net.VpnService"/>
            </intent-filter>
            <meta-data android:name="android.net.VpnService.SUPPORTS_ALWAYS_ON"
                android:value="true"/>

        </service>

        <service
            android:name=".QuickConnectTileService"
            android:exported="true"
            android:icon="@drawable/ic_tile_quick_on"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
            <!-- active: the app pushes state (requestListeningState) instead of the shade
                 binding the tile on every open; toggleable: accessibility reads it as a switch;
                 category: where the Quick Settings edit page files it (Android 16 QPR2+) -->
            <meta-data
                android:name="android.service.quicksettings.ACTIVE_TILE"
                android:value="true" />
            <meta-data
                android:name="android.service.quicksettings.TOGGLEABLE_TILE"
                android:value="true" />
            <meta-data
                android:name="android.service.quicksettings.TILE_CATEGORY"
                android:value="android.service.quicksettings.CATEGORY_CONNECTIVITY" />
        </service>

        <receiver
            android:name=".StartReceiver"
            android:directBootAware="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.MY_PACKAGE_UNSUSPENDED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".NotificationDisconnectReceiver"
            android:exported="false" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data
                android:host="details"
                android:scheme="solanadappstore" />
        </intent>
        <!-- visibility for launchable apps, used by the app split rules screen -->
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

</manifest>
